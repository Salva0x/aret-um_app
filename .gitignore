# Ignore entries satisfying the following rules:
#   - If the entry is a directory, ignore the directory and its contents.
#   - If the entry is a file, ignore the file.

# General
# -------
# Compiled class files
*.class
# Log files
*.log
# Python bytecode
*.pyc
# Vim swap files
*.swp
# macOS specific
.DS_Store
# Temporary files
*~
# Editor specific
.history

# IDE specific
# ------------
# IntelliJ / Android Studio
.idea/
*.iml
*.ipr
*.iws
# VS Code - uncomment if you DON'T want to share launch configurations
#.vscode/

# Flutter & Dart
# --------------
# Generated by Pub
.dart_tool/
.packages
# .pub-cache is a global directory but linking here for completeness
.pub-cache/
# Build directory
build/
# Generated plugin registrant
.flutter-plugins
.flutter-plugins-dependencies
# Generated API documentation
**/doc/api/
# Flutter tool state
.flutter-plugins-dependencies
.toolLogs/

# Android specific
# ----------------
# Local properties (SDK path, etc.) 
android/local.properties
# Gradle cache
android/.gradle/
# Android Studio build output
android/app/build/
# Android Studio generated files
android/app/debug/
android/app/profile/
android/app/release/
# Keystore files
# Ensure these patterns match your keystore file names and locations
*.jks
android/key.properties
android/app/key.properties
# Android NDK profiling data
android/app/src/main/gdb.setup
android/app/src/main/lldb_settings.ini
# Android Studio profiling data
android/captures/
# Android Studio generated files
android/app/generated/
# Android aar plugins
android/.idea/libraries

# iOS specific
# ------------
# Xcode specific
ios/*.xcscheme
ios/Runner.xcworkspace/contents.xcworkspacedata
ios/Runner.xcworkspace/xcshareddata/
ios/Runner.xcodeproj/project.xcworkspace/
ios/Runner.xcodeproj/xcuserdata/
# CocoaPods
ios/Pods/
# Generated by Flutter
ios/Flutter/App.framework/
ios/Flutter/Flutter.framework/
ios/Flutter/engine.xcconfig
ios/Flutter/.last_build_id
ios/Flutter/flutter_export_environment.sh
# Generated plugin registrant
ios/Runner/GeneratedPluginRegistrant.*
# Build artifacts
ios/build/
# Derived data
ios/DerivedData/
# Carthage
ios/Carthage/

# Web specific (if you build for web)
# -----------------------------------
# Build directory for web
web/build/
# Entrypoint file generated by Flutter for web
web/index.html.dart

# Desktop specific (if you build for desktop)
# -------------------------------------------
# Linux
linux/build/
linux/flutter/ephemeral/
# macOS
macos/build/
macos/Flutter/ephemeral/
# Windows
windows/build/
windows/flutter/ephemeral/
windows/runner/Debug/
windows/runner/Profile/
windows/runner/Release/
windows/runner/flutter_build.generated.props
windows/runner/INSTALL.vcxproj.filters
windows/runner/AreteumApp.vcxproj.filters # Replace AreteumApp with your app name if different
windows/runner/vc.db
windows/runner/.vs/

# Sensitive or large files that should not be versioned
# -----------------------------------------------------
# Example: Large media files, datasets, etc.
# *.mp4
# *.zip
# credentials.json # Any other custom credential files

# Firebase configuration files (usually versioned, but can be ignored if sensitive or per-environment)
# If you have different Firebase configs for dev/staging/prod and don't want to commit them all,
# or if they contain highly sensitive info not protected by Firebase rules.
# However, for ease of setup, these are typically included.
# android/app/google-services.json
# ios/Runner/GoogleService-Info.plist

# Pubspec.lock and Podfile.lock are intentionally NOT ignored.
# It's a best practice to commit these files to ensure consistent dependency versions
# across different environments and collaborators.
# pubspec.lock
# ios/Podfile.lock